library(UWBiost561)
set.seed(0)
simulation <- UWBiost561::generate_partial_clique(
n = 10,
clique_fraction = 0.5,
clique_edge_density = 0.9
)
adj_mat <- simulation$adj_mat
library(UWBiost561)
set.seed(0)
simulation <- UWBiost561::generate_partial_clique(
n = 10,
clique_fraction = 0.5,
clique_edge_density = 0.9
)
simulation$adj_mat
?lapply()
candidate_densities <- sapply(candidate_results, function(one_candidate) one_candidate$density)
compute_maximal_partial_clique <- function(adj_mat, alpha) {
stopifnot(
is.numeric(alpha),
length(alpha) == 1,
!is.na(alpha),
alpha >= 0.5,
alpha <= 1,
is.matrix(adj_mat),
nrow(adj_mat) == ncol(adj_mat),
nrow(adj_mat) >= 5,
nrow(adj_mat) <= 50,
all(adj_mat %in% c(0, 1)),
all(adj_mat == t(adj_mat)),
all(diag(adj_mat) == 1),
is.null(rownames(adj_mat)),
is.null(colnames(adj_mat))
)
n <- nrow(adj_mat)
compute_density <- function(idx) {
m <- length(idx)
if (m <= 1) {
return(1)
}
num_edge <- (sum(adj_mat[idx, idx]) - m) / 2
max_edge <- m * (m - 1) / 2
return(num_edge / max_edge)
}
best_clique <- 1
best_density <- 1
for (start_node in 1:n) {
current_clique <- start_node
remaining_nodes <- setdiff(1:n, current_clique)
keep_going <- TRUE
while (length(remaining_nodes) > 0 && keep_going) {
candidate_results <- lapply(remaining_nodes, function(candidate_node) {
candidate_clique <- c(current_clique, candidate_node)
candidate_density <- compute_density(candidate_clique)
list(
node = candidate_node,
clique = candidate_clique,
density = candidate_density
)
})
candidate_densities <- sapply(
candidate_results,
function(one_candidate) one_candidate$density
)
valid_candidates <- which(candidate_densities >= alpha)
if (length(valid_candidates) == 0) {
keep_going <- FALSE
} else {
best_candidate_position <- valid_candidates[
which.max(candidate_densities[valid_candidates])
]
current_clique <- candidate_results[[best_candidate_position]]$clique
remaining_nodes <- setdiff(1:n, current_clique)
}
}
current_density <- compute_density(current_clique)
if (length(current_clique) > length(best_clique)) {
best_clique <- current_clique
best_density <- current_density
} else if (length(current_clique) == length(best_clique) &&
current_density > best_density) {
best_clique <- current_clique
best_density <- current_density
}
}
best_clique <- sort(unique(best_clique))
best_density <- compute_density(best_clique)
return(list(
clique_idx = best_clique,
edge_density = best_density
))
}
adj_mat[1:5, 1:5] <- 1
devtools::install()
devtools::document()
library(UWBiost561)
set.seed(0)
simulation <- UWBiost561::generate_partial_clique(
n = 10,
clique_fraction = 0.5,
clique_edge_density = 0.9
)
adj_mat <- simulation$adj_mat
res <- UWBiost561::compute_maximal_partial_clique(
adj_mat = adj_mat,
alpha = 0.9
)
devtools::install()
library(UWBiost561)
list.files("R")
# 2. Update documentation and NAMESPACE
devtools::document()
library(UWBiost561)
Session → Restart R
remove.packages("UWBiost561")
getwd()
list("R")
list.files("R")
devtools::document()
devtools::install()
.rs.restartR()
library(UWBiost561)
"generate_partial_clique" %in% getNamespaceExports("UWBiost561")
"compute_maximal_partial_clique" %in% getNamespaceExports("UWBiost561")
knitr::opts_chunk$set(
collapse = TRUE,
comment = "#>"
)
set.seed(10)
library(UWBiost561)
set.seed(0)
simulation <- UWBiost561::generate_partial_clique(
n = 10,
clique_fraction = 0.5,
clique_edge_density = 0.9
)
adj_mat <- simulation$adj_mat
res <- UWBiost561::compute_maximal_partial_clique(
adj_mat = adj_mat,
alpha = 0.9
)
res
#' matrix containing only 0s and 1s, with 1s on the diagonal and no row or column names.
#' @export
#'
#' @examples
#' output <- generate_partial_clique(
#'   n = 10,
#'   clique_fraction = 0.4,
#'   clique_edge_density = 0.8
#' )
#'
generate_partial_clique <- function(n = 50,
clique_fraction = 0.2,
clique_edge_density = 1,
density_low = 0.1) {
stopifnot(
is.numeric(n),
length(n) == 1,
!is.na(n),
n %% 1 == 0,
n > 0,
is.numeric(clique_fraction),
length(clique_fraction) == 1,
!is.na(clique_fraction),
clique_fraction >= 0,
clique_fraction <= 1,
is.numeric(clique_edge_density),
length(clique_edge_density) == 1,
!is.na(clique_edge_density),
clique_edge_density >= 0,
clique_edge_density <= 1,
is.numeric(density_low),
length(density_low) == 1,
!is.na(density_low),
density_low >= 0,
density_low <= 1
)
adj_mat <- matrix(
sample(
x = c(0, 1),
size = n^2,
prob = c(1 - density_low, density_low),
replace = TRUE
),
nrow = n,
ncol = n
)
adj_mat <- adj_mat + t(adj_mat)
adj_mat[adj_mat > 0] <- 1
diag(adj_mat) <- 1
clique_size <- round(n * clique_fraction)
if (clique_size >= 2) {
possible_edges <- t(combn(1:clique_size, 2))
max_edges <- clique_size * (clique_size - 1) / 2
n_clique_edges <- round(clique_edge_density * max_edges)
adj_mat[1:clique_size, 1:clique_size] <- 0
diag(adj_mat)[1:clique_size] <- 1
if (n_clique_edges > 0) {
selected_edges <- possible_edges[
sample(seq_len(max_edges), size = n_clique_edges, replace = FALSE),
,
drop = FALSE
]
for (i in seq_len(nrow(selected_edges))) {
node1 <- selected_edges[i, 1]
node2 <- selected_edges[i, 2]
adj_mat[node1, node2] <- 1
adj_mat[node2, node1] <- 1
}
}
}
sample_idx <- sample(seq_len(n))
adj_mat <- adj_mat[sample_idx, sample_idx]
rownames(adj_mat) <- NULL
colnames(adj_mat) <- NULL
return(list(adj_mat = adj_mat))
}
devtools::document()
devtools::install()
library(UWbiost561)
library(UWbiost561)
library(UWBiost561)
generate_partial_clique
devtools::test()
> devtools::test()
devtools::test()
usethis::use_mit_license()
devtools::check()
devtools::session_info()
rmarkdown::render("vignettes/HW3.Rmd")
diag(adj_mat)[1:clique_size] <- 1
#' matrix containing only 0s and 1s, with 1s on the diagonal and no row or column names.
#' @export
#'
#' @examples
#' output <- generate_partial_clique(
#'   n = 10,
#'   clique_fraction = 0.4,
#'   clique_edge_density = 0.8
#' )
#'
generate_partial_clique <- function(n = 50,
clique_fraction = 0.2,
clique_edge_density = 1,
density_low = 0.1) {
stopifnot(
is.numeric(n),
length(n) == 1,
!is.na(n),
n %% 1 == 0,
n > 0,
is.numeric(clique_fraction),
length(clique_fraction) == 1,
!is.na(clique_fraction),
clique_fraction >= 0,
clique_fraction <= 1,
is.numeric(clique_edge_density),
length(clique_edge_density) == 1,
!is.na(clique_edge_density),
clique_edge_density >= 0,
clique_edge_density <= 1,
is.numeric(density_low),
length(density_low) == 1,
!is.na(density_low),
density_low >= 0,
density_low <= 1
)
adj_mat <- matrix(
sample(
x = c(0, 1),
size = n^2,
prob = c(1 - density_low, density_low),
replace = TRUE
),
nrow = n,
ncol = n
)
adj_mat <- adj_mat + t(adj_mat)
adj_mat[adj_mat > 0] <- 1
diag(adj_mat) <- 1
clique_size <- round(n * clique_fraction)
if (clique_size >= 2) {
possible_edges <- t(utils::combn(1:clique_size, 2))
max_edges <- clique_size * (clique_size - 1) / 2
n_clique_edges <- round(clique_edge_density * max_edges)
adj_mat[1:clique_size, 1:clique_size] <- 0
diag(adj_mat)[1:clique_size] <- 1
if (n_clique_edges > 0) {
selected_edges <- possible_edges[
sample(seq_len(max_edges), size = n_clique_edges, replace = FALSE),
,
drop = FALSE
]
for (i in seq_len(nrow(selected_edges))) {
node1 <- selected_edges[i, 1]
node2 <- selected_edges[i, 2]
adj_mat[node1, node2] <- 1
adj_mat[node2, node1] <- 1
}
}
}
sample_idx <- sample(seq_len(n))
adj_mat <- adj_mat[sample_idx, sample_idx]
rownames(adj_mat) <- NULL
colnames(adj_mat) <- NULL
return(list(adj_mat = adj_mat))
}
knitr::opts_chunk$set(
collapse = TRUE,
comment = "#>"
)
set.seed(10)
source("https://raw.githubusercontent.com/linnykos/561_s2026_public/main/HW2_files/random_graph_functions.R")
generate_random_graph
knitr::include_graphics("https://raw.githubusercontent.com/linnykos/561_s2026_public/main/HW3_files/partial-clique.png")
# This is NOT a good implementation.
# This is solely for demonstration.
# This lousy function simply picks a random set of nodes.
# It doesn't actually compute the valid partial clique!
compute_maximal_partial_clique <- function(adj_mat, alpha){
n <- nrow(adj_mat)
clique_idx <- sample(1:n, size = ceiling(n/2))
m <- length(clique_idx)
edge_density_numerator <- (sum(adj_mat[idx,idx]) - m)/2
edge_density_denominator <- m*(m-1)/2
edge_density <- edge_density_numerator/edge_density_denominator
return(list(clique_idx = clique_idx,
edge_density = edge_density))
}
knitr::include_graphics("https://github.com/linnykos/561_s2026_public/blob/main/HW3_files/check1_editted.png?raw=true")
knitr::include_graphics("https://github.com/linnykos/561_s2026_public/blob/main/HW3_files/check2_editted.png?raw=true")
knitr::include_graphics("figures/check_first_20.png")
knitr::include_graphics("figures/check_last_20.png")
devtools::session_info()
library(UWBiost561)
knitr::opts_chunk$set(
collapse = TRUE,
comment = "#>"
)
set.seed(10)
knitr::opts_chunk$set(
collapse = TRUE,
comment = "#>"
)
set.seed(10)
source("https://raw.githubusercontent.com/linnykos/561_s2026_public/main/HW2_files/random_graph_functions.R")
generate_random_graph
devtools::install()
library(UWBiost561)
set.seed(0)
simulation <- UWBiost561::generate_partial_clique(
n = 10,
clique_fraction = 0.5,
clique_edge_density = 0.9
)
simulation$adj_mat
library(UWBiost561)
set.seed(0)
simulation <- UWBiost561::generate_partial_clique(
n = 10,
clique_fraction = 0.5,
clique_edge_density = 0.9
)
adj_mat <- simulation$adj_mat
res <- UWBiost561::compute_maximal_partial_clique(
adj_mat = adj_mat,
alpha = 0.9
)
res
# This is what your testthat.R file should contain
library(testthat)
library(UWBiost561)
testthat::test_check("UWBiost561")
library(testthat)
devtools::load_all()
devtools::check()
devtools::test()
knitr::opts_chunk$set(
collapse = TRUE,
comment = "#>"
)
set.seed(10)
library(testthat)
devtools::build()
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
adj_mat
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
devtools::document()
devtools::build()
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
devtools::check()
devtools::build()
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
devtools::document()
devtools::load_all()
UWBiost561::compute_maximal_partial_clique_master
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
names(result_list) <- paste("Implementation:", imp_numbers)
set.seed(10)
library(UWBiost561)
data <- UWBiost561::generate_partial_clique(n = 10,
clique_fraction = 0.9,
clique_edge_density = 0.9)
adj_mat <- data$adj_mat
imp_numbers <- 1:9
result_list <- lapply(imp_numbers, function(imp_number){
print(paste("Working on implementation:", imp_number))
result <- UWBiost561::compute_maximal_partial_clique_master(adj_mat = adj_mat,
alpha = 0.95,
number = imp_number,
time_limit = 30)
return(result)
})
names(result_list) <- paste("Implementation:", imp_numbers)
# print out all the results
result_list
